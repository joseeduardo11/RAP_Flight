projection;
strict ( 2 );
use draft;

define behavior for ZC_FE_Travel alias Travel
use etag
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;
}